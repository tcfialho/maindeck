diff --git a/protocol/river-window-management-v1.xml b/protocol/river-window-management-v1.xml
index a9a066e..fa7ecba 100644
--- a/protocol/river-window-management-v1.xml
+++ b/protocol/river-window-management-v1.xml
@@ -34,7 +34,7 @@
     document are to be interpreted as described in IETF RFC 2119.
   </description>
 
-  <interface name="river_window_manager_v1" version="5">
+  <interface name="river_window_manager_v1" version="6">
     <description summary="window manager global interface">
       This global interface should only be advertised to the window manager
       process. Only one window management client may be active at a time. The
@@ -42,14 +42,14 @@
 
       There are two disjoint categories of state managed by this protocol:
 
-      Window management state influences the communication between the server
-      and individual window clients (e.g. xdg_toplevels). Window management
+      Window management state influences the communication between the
+      compositor and individual windows (e.g. xdg_toplevels). Window management
       state includes window dimensions, fullscreen state, keyboard focus,
       keyboard bindings, and more.
 
       Rendering state only affects the rendered output of the compositor and
-      does not influence communication between the server and individual window
-      clients. Rendering state includes the position and rendering order of
+      does not influence communication between the compositor and individual
+      windows. Rendering state includes the position and rendering order of
       windows, shell surfaces, decoration surfaces, borders, and more.
 
       Window management state may only be modified by the window manager as part
@@ -211,8 +211,8 @@
 
     <event name="render_start">
       <description summary="start a render sequence">
-        This event indicates that the server has sent all river_node_v1.position
-        and river_window_v1.dimensions events necessary.
+        This event indicates that the server has sent all
+        river_window_v1.dimensions events necessary.
 
         In response to this event, the client should make requests modifying
         rendering state as it chooses. Then, the client must make the
@@ -319,7 +319,7 @@
     </request>
   </interface>
 
-  <interface name="river_window_v1" version="5">
+  <interface name="river_window_v1" version="6">
     <description summary="a logical window">
       This represents a logical window. For example, a window may correspond to
       an xdg_toplevel or Xwayland window.
@@ -620,6 +620,11 @@
         The color is defined by four 32-bit RGBA values. Unless specified in
         another protocol extension, the RGBA values use pre-multiplied alpha.
 
+        The valid range for the RGBA values is from 0x00000000 to 0xffffffff.
+        These values are interpreted as a percentage:
+        - 0x00000000 means 0% of the given color component
+        - 0xffffffff means 100% of the given color component
+
         Setting the edges to none or the width to 0 disables the borders.
         Setting a negative width is a protocol error.
 
@@ -661,7 +666,7 @@
     </request>
 
     <request name="get_decoration_above">
-      <description summary="create a decoration surface above the window">
+      <description summary="create a decoration above the window in z-order">
         Create a decoration surface and assign the river_decoration_v1 role to
         the surface. The created decoration is placed above the window in
         rendering order, see the description of river_decoration_v1.
@@ -676,7 +681,7 @@
     </request>
 
     <request name="get_decoration_below">
-      <description summary="create a decoration surface below the window">
+      <description summary="create a decoration below the window in z-order">
         Create a decoration surface and assign the river_decoration_v1 role to
         the surface. The created decoration is placed below the window in
         rendering order, see the description of river_decoration_v1.
@@ -816,7 +821,7 @@
         maximized.
 
         The window manager is free to honor this request using
-        river_window_v1.inform_maximize or ignore it.
+        river_window_v1.inform_maximized or ignore it.
 
         This event will be followed by a manage_start event after all other new
         state has been sent by the server.
@@ -1130,6 +1135,59 @@
       </description>
       <arg name="count" type="uint" summary="count of screen capture sessions"/>
     </event>
+
+    <enum name="animation_intent">
+      <description summary="animation intent enum for this window action">
+        Intent declarations allow the WM to explicitly specify which animation
+        effect should be applied to this window. The compositor uses this to
+        select easing, duration, and visual effect (fade/slide/reflow/spring).
+      </description>
+      <entry name="none" value="0x0" summary="no animation"/>
+      <entry name="fade_open" value="0x1" summary="open solo: scale fade in"/>
+      <entry name="fade_close" value="0x2" summary="close solo: scale fade out"/>
+      <entry name="slide_close" value="0x3" summary="close main: slide left"/>
+      <entry name="slide_deck_out" value="0x4" summary="close deck: slide right"/>
+      <entry name="slide_in" value="0x5" summary="open group: slide in left"/>
+      <entry name="reflow_ease" value="0x6" summary="geometry only, ease-in-out"/>
+      <entry name="spring" value="0x7" summary="spring geometry change"/>
+      <entry name="nudge" value="0x8" summary="focus nudge: small lateral translate"/>
+      <entry name="fs_carousel" value="0x9" summary="fullscreen carousel switch"/>
+      <entry name="minimize" value="0xa" summary="minimize: scale fade down"/>
+      <entry name="unminimize" value="0xb" summary="unminimize: scale fade up"/>
+    </enum>
+
+    <enum name="animation_easing">
+      <description summary="easing function selector">
+        Easing curves applied during animation. The compositor interprets these
+        and applies the corresponding easing function to the animation progress.
+      </description>
+      <entry name="linear" value="0x0" summary="linear interpolation"/>
+      <entry name="ease_in" value="0x1" summary="quadratic ease in"/>
+      <entry name="ease_out" value="0x2" summary="quadratic ease out"/>
+      <entry name="ease_in_out" value="0x3" summary="quadratic ease in-out"/>
+      <entry name="cubic_spring" value="0x4" summary="cubic spring (0.22,1,0.36,1)"/>
+    </enum>
+
+    <request name="set_animation_intent" since="6">
+      <description summary="declare animation intent for this window action">
+        Inform the compositor of the intended animation effect for this window
+        state change. The compositor will select the appropriate visual effect
+        (fade, slide, reflow, spring, etc.) based on the intent.
+
+        The intent is applied to the next render_finish following this request.
+        Specifying NONE (0x0) indicates no animation should be applied.
+
+        Duration in milliseconds. If 0, the intent's default duration is used.
+        Easing selects the interpolation curve. If not specified, the intent's
+        default easing is used.
+
+        This request modifies rendering state and may only be made as part of
+        a manage or render sequence, see the river_window_manager_v1 description.
+      </description>
+      <arg name="intent" type="uint" enum="animation_intent" summary="animation intent"/>
+      <arg name="duration_ms" type="uint" summary="duration in milliseconds, 0 for default"/>
+      <arg name="easing" type="uint" enum="animation_easing" summary="easing function, 0 for default"/>
+    </request>
   </interface>
 
   <interface name="river_decoration_v1" version="5">
@@ -1372,7 +1430,7 @@
         mapping between wl_output and river_output_v1 objects.
 
         The global_remove event for the corresponding wl_output may be sent
-        before the river_output_v1.remove event. This is due to the fact that
+        before the river_output_v1.removed event. This is due to the fact that
         river_output_v1 state changes are synced to the river window management
         manage sequence while changes to globals are not.
 
@@ -1530,7 +1588,7 @@
         mapping between wl_seat and river_seat_v1 objects.
 
         The global_remove event for the corresponding wl_seat may be sent before
-        the river_seat_v1.remove event. This is due to the fact that
+        the river_seat_v1.removed event. This is due to the fact that
         river_seat_v1 state changes are synced to the river window management
         manage sequence while changes to globals are not.
 
diff --git a/wm-animation-intents.c b/wm-animation-intents.c
index 2a71bdf..67b9330 100644
--- a/wm-animation-intents.c
+++ b/wm-animation-intents.c
@@ -1,6 +1,8 @@
 #include <stdint.h>
 #include <string.h>
 
+#include <river-window-management-v1-client-protocol.h>
+
 #include "wm-animation-intents.h"
 #include "wm-log.h"
 #include "wm-state.h"
@@ -119,15 +121,23 @@ const char *md_animation_intent_name(AnimationIntent intent) {
 }
 
 /* Send animation intent to compositor via protocol.
- * This is a no-op stub for now — the real implementation will call
- * river_window_v1_set_animation_intent(window, intent, duration, easing)
- * once the protocol bindings are generated. */
+ * Requires the window object bound at protocol v6 (set_animation_intent is
+ * since=6). The compositor applies the intent to the NEXT render_finish for this
+ * window, so this must be called within the same manage/render sequence as the
+ * geometry/visibility change it describes (which is how wm-layout.c calls it). */
 void md_send_animation_intent(struct Window *window, AnimationIntent intent) {
     if (!window || !window->obj) {
         LOG_WARN("md_send_animation_intent: invalid window");
         return;
     }
 
+    /* The request is since=6. If the compositor only offered an older version,
+     * the proxy lacks the opcode and calling it would be a protocol error; skip
+     * silently so an old compositor still works (animations just don't fire). */
+    if (wl_proxy_get_version((struct wl_proxy *)window->obj) < 6) {
+        return;
+    }
+
     struct IntentConfig config = intentConfig(intent);
 
     if (md_verbose()) {
@@ -138,8 +148,10 @@ void md_send_animation_intent(struct Window *window, AnimationIntent intent) {
                   config.easing);
     }
 
-    /* TODO: Call river_window_v1_set_animation_intent(window->obj, intent, config.duration_ms, config.easing);
-     * once protocol bindings are generated. For now, this is a stub that logs the intent. */
+    river_window_v1_set_animation_intent(window->obj,
+                                         (uint32_t)intent,
+                                         config.duration_ms,
+                                         (uint32_t)config.easing);
 }
 
 /* Helper: determine intent based on acion context.
diff --git a/wm-handlers.c b/wm-handlers.c
index 73fb258..5d6c207 100644
--- a/wm-handlers.c
+++ b/wm-handlers.c
@@ -965,8 +965,12 @@ static void handle_global(void *data, struct wl_registry *registry, uint32_t nam
 		const char *interface, uint32_t version) {
 	if (strcmp(interface, river_window_manager_v1_interface.name) == 0) {
 		if (version >= 4) {
+			/* Bind the highest version we and the compositor share. v6 adds
+			 * set_animation_intent (the P17 directional slides); degrade
+			 * gracefully to 5/4 against an older compositor. */
+			uint32_t bind_ver = version >= 6 ? 6 : (version >= 5 ? 5 : 4);
 			window_manager_v1 = wl_registry_bind(registry, name, &river_window_manager_v1_interface,
-				version >= 5 ? 5 : 4);
+				bind_ver);
 		}
 	} else if (strcmp(interface, river_xkb_bindings_v1_interface.name) == 0) {
 		xkb_bindings_v1 = wl_registry_bind(registry, name, &river_xkb_bindings_v1_interface, 1);
